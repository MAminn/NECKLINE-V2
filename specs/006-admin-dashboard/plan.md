# Implementation Plan: Admin Dashboard

**Branch**: `006-admin-dashboard` | **Date**: 2026-06-05 | **Spec**: [spec.md](./spec.md)

**Input**: Feature specification from `specs/006-admin-dashboard/spec.md`

---

## Summary

Build a full 10-tab protected admin portal that matches the recovered `NicklineAdminPortal.tsx` UI design. The admin frontend lives at `/admin` inside the existing Next.js app, gated by role-based auth. The backend adds new Mongoose models (`Testimonial`, `HeaderSlide`, `HowToApply`), a `fulfillmentStatus` field on `Order`, and a suite of admin API endpoints. All admin mutations are audit-logged. Public content endpoints (`/testimonials`, `/header-slides`, `/how-to-apply`) serve the storefront without authentication.

---

## Technical Context

**Language/Version**: TypeScript (Next.js frontend), JavaScript (Express API), Node 20

**Primary Dependencies**: Next.js 15, React 18, Tailwind CSS, Framer Motion, Mongoose 8, Express 4, Lucide React (icons — already installed)

**Storage**: MongoDB (existing) — 3 new collections: `testimonials`, `headerslides`, `howtoapplies`

**Testing**: Jest (existing API test suites), manual E2E via quickstart.md

**Target Platform**: Web — desktop-first admin UI; same deployment as storefront

**Performance Goals**: Dashboard KPI cards load in < 2s; paginated lists at 8–20 rows; no unbounded scans

**Constraints**: Must match recovered UI design token-for-token. No additional npm packages unless essential.

**Scale/Scope**: MVP scale (~200 products, ~500 orders, ~150 customers); MongoDB aggregation is acceptable without a dedicated analytics store.

---

## Constitution Check

| Gate | Status | Notes |
|------|--------|-------|
| §III Server authority | ✅ Pass | All prices, totals, and stock computed server-side; admin UI sends IDs + mutations only |
| §IV.3 Authorization | ✅ Pass | `requirePermission('admin:access')` on every admin endpoint; UI gate is secondary |
| §IV.4 Rate limiting | ✅ Pass | Admin endpoints get a dedicated stricter rate limiter |
| §V.4 Historical integrity | ✅ Pass | Product delete is soft-delete (`deletedAt`); order line-item snapshots untouched |
| §VIII.3 Audit trail | ✅ Pass | All admin mutations emit `createAuditEvent`; Activity Log reads from `AuditEvent` collection |
| §X.3 Pagination | ✅ Pass | All list endpoints paginated; no unbounded collection scans |
| §XII File uploads | ✅ Pass | Images stored as URLs; upload proxy stores outside runtime filesystem |
| §2.1 Design fidelity | ✅ Pass | UI matches recovered `NicklineAdminPortal.tsx` design token-for-token |

---

## Project Structure

### Documentation (this feature)

```text
specs/006-admin-dashboard/
├── plan.md              ← this file
├── research.md          ← decisions and rationale
├── data-model.md        ← new/modified schemas
├── quickstart.md        ← validation walkthrough
├── contracts/
│   └── admin-api.md     ← all endpoint contracts
└── tasks.md             ← generated by /speckit-tasks
```

### Backend — new files

```text
apps/api/src/
├── models/
│   ├── Testimonial.js              ← new
│   ├── HeaderSlide.js              ← new
│   └── HowToApply.js               ← new
├── services/
│   ├── adminMetricsService.js      ← new (MongoDB aggregation)
│   └── uploadService.js            ← new (Cloudinary proxy, optional)
├── routes/v1/admin/
│   ├── products.js                 ← new
│   ├── orders.js                   ← new
│   ├── customers.js                ← new
│   ├── coupons.js                  ← new (PromoCode isAutomatic:false)
│   ├── offers.js                   ← new (PromoCode isAutomatic:true)
│   ├── metrics.js                  ← new
│   ├── activities.js               ← new
│   ├── uploads.js                  ← new
│   └── activityLog.js              ← new
└── routes/v1/
    ├── testimonials.js             ← new
    ├── headerSlides.js             ← new
    └── howToApply.js               ← new
```

### Backend — modified files

```text
apps/api/src/
├── models/Order.js         ← add fulfillmentStatus, trackingNumber
├── models/Product.js       ← add views, sales, subtitle
├── middleware/rateLimiters/ ← add rateLimiterAdmin
└── routes/v1/index.js      ← register all new routes
```

### Frontend — new files

```text
apps/web/src/
├── app/admin/
│   ├── layout.tsx                      ← sidebar shell + auth gate
│   ├── page.tsx                        ← redirect → /admin/dashboard
│   ├── dashboard/page.tsx
│   ├── products/page.tsx
│   ├── orders/page.tsx
│   ├── customers/page.tsx
│   ├── analytics/page.tsx
│   ├── offers/page.tsx
│   ├── reports/page.tsx                ← placeholder
│   ├── reviews/page.tsx
│   ├── interface/page.tsx
│   └── settings/
│       ├── page.tsx                    ← redirect → /admin/settings/profile
│       ├── profile/page.tsx
│       └── activity-log/page.tsx
├── components/admin/
│   ├── AdminSidebar.tsx
│   ├── AdminKpiCard.tsx                ← KPI card with SVG sparkline
│   ├── AdminModal.tsx                  ← reusable modal wrapper
│   ├── AdminImageUploader.tsx          ← URL input + optional file upload
│   ├── dashboard/
│   │   ├── DashboardKpis.tsx
│   │   ├── TopProductsList.tsx
│   │   ├── RecentOrdersTable.tsx
│   │   └── ActivityFeed.tsx
│   ├── products/
│   │   ├── ProductsTable.tsx
│   │   ├── AddProductModal.tsx
│   │   └── EditProductModal.tsx
│   ├── orders/
│   │   ├── OrdersTable.tsx
│   │   └── OrderDetailSidebar.tsx
│   ├── customers/
│   │   ├── CustomersTable.tsx
│   │   └── CustomerDetailSidebar.tsx
│   ├── offers/
│   │   ├── CouponsSection.tsx
│   │   └── CampaignsSection.tsx
│   ├── reviews/
│   │   ├── ReviewsTable.tsx
│   │   ├── AddReviewModal.tsx
│   │   └── EditReviewModal.tsx
│   ├── interface/
│   │   ├── HeroSlidesSection.tsx
│   │   ├── SlideModal.tsx
│   │   └── HowToApplyEditor.tsx
│   └── analytics/
│       └── AnalyticsChart.tsx
└── lib/
    └── admin-api.ts                    ← typed fetch wrappers for all admin endpoints
```

### Frontend — modified files

```text
apps/web/src/
├── styles/globals.css     ← add admin CSS custom properties
└── types/nickline.ts      ← add Testimonial, HowToApply, AdminMetrics types
```

---

## Implementation Phases

### Phase A — Backend Models & Content Endpoints
New models + public read + admin CRUD for Testimonial, HeaderSlide, HowToApply.

### Phase B — Admin Infrastructure Endpoints
Admin-only endpoints: products, orders, customers, coupons, offers, metrics, activities, activity-log, uploads. Apply admin rate limiter.

### Phase C — Frontend Shell
`/admin/layout.tsx` auth gate, `AdminSidebar`, token additions to globals.css, `admin-api.ts`.

### Phase D — Dashboard Tab (P1)
KPI cards with sparklines, Top Products, Recent Orders, Activity Feed.

### Phase E — Products Tab (P1)
Paginated table, Add/Edit modals with 3-image gallery, delete.

### Phase F — Orders Tab (P1)
Status filter tabs, table, detail sidebar with fulfillment transitions + tracking.

### Phase G — Customers, Offers, Reviews, Interface Billboard (P2)
All four P2 tabs wired to their respective API endpoints.

### Phase H — Analytics & Settings (P3)
Analytics chart, Profile update, Activity Log read-only table, Reports placeholder.

### Phase I — Polish & Validation
Audit events on all mutations, lint, build, quickstart walkthrough, security validation.

---

## Complexity Tracking

No constitution violations. All decisions within the established MVP MERN stack.
